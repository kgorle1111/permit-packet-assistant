# Nothing is required to run the tests. For the live demo:
ANTHROPIC_API_KEY=            # intake extraction; model: claude-sonnet-5 (~$0.01-0.02/turn)

# Optional — real SMS to homeowners (client-owned); without these, messages
# still persist to messages.jsonl and the demo runs dry
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Value receipt
MIN_PER_FIELD=1.5             # consultant's own re-keying minutes per form field — time it in week one
